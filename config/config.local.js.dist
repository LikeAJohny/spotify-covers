const express = require('express');
const path = require('path');

module.exports = {
    staticFiles: express.static(''),
    auth: {
        clientId: '',
        clientSecret: '',
        authUri: '',
        grantType: '',
        tokenStore: '',
    }
};